{
  "devCommand": "uvicorn mainraw:app --host 0.0.0.0 --port 3000",
  "builds": [
    {
      "src": "mainraw.py",
      "use": "@now/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}

